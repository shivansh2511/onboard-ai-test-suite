import React from 'react';
// Anonymous function declaration default export in an index file
export default function() {
  const handleLogin = () => {};
  return <button onClick={handleLogin}>Login</button>;
}